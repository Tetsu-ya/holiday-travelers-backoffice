@extends('layouts.app')
@section('title', $user->name)
@section('content')
<div class="max-w-4xl space-y-5"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm text-gray-500">Staff profile</p><h2 class="font-heading text-2xl font-semibold text-primary">{{ $user->name }}</h2><p class="text-sm text-gray-500">{{ $user->email }}</p></div><div class="flex gap-3"><a href="{{ route('staff-profiles.index') }}" class="group inline-flex items-center gap-2 rounded-xl border border-border px-4 py-2.5 text-sm font-semibold text-primary transition-all duration-200 hover:-translate-y-0.5 hover:border-secondary hover:text-secondary active:translate-y-0 active:scale-95"><span class="transition-transform duration-200 group-hover:-translate-x-1">←</span><span>Back</span></a><a href="{{ route('staff-profiles.edit', $user) }}" class="group inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-white shadow-lg shadow-primary/15 transition-all duration-200 hover:-translate-y-0.5 hover:bg-secondary hover:shadow-xl active:translate-y-0 active:scale-95"><span>Edit</span><span class="transition-transform duration-200 group-hover:translate-x-1">→</span></a></div></div>
<div class="rounded-2xl border border-border bg-card p-6 shadow-sm"><dl class="grid gap-5 md:grid-cols-2"><div><dt class="text-sm text-gray-500">Role</dt><dd class="font-medium capitalize">{{ $user->role ?? 'staff' }}</dd></div><div><dt class="text-sm text-gray-500">Status</dt><dd class="font-medium capitalize">{{ $user->status ?? 'active' }}</dd></div><div><dt class="text-sm text-gray-500">Department</dt><dd>{{ $user->department ?: 'Not assigned' }}</dd></div><div><dt class="text-sm text-gray-500">Job title</dt><dd>{{ $user->job_title ?: 'Not assigned' }}</dd></div><div><dt class="text-sm text-gray-500">Phone</dt><dd>{{ $user->phone ?: 'Not provided' }}</dd></div><div><dt class="text-sm text-gray-500">Member since</dt><dd>{{ $user->created_at?->format('d M Y') }}</dd></div></dl></div>
@if (auth()->id() !== $user->id)<div class="flex items-center justify-between gap-4 rounded-2xl border border-error/20 bg-error/[0.03] p-4"><div><p class="text-sm font-semibold text-primary">Danger zone</p><p class="mt-1 text-xs text-gray-500">Removing this profile will permanently delete the staff account.</p></div><form method="POST" action="{{ route('staff-profiles.destroy', $user) }}" onsubmit="return confirm('Remove this staff profile? This action cannot be undone.')">@csrf @method('DELETE')<button type="submit" class="group inline-flex shrink-0 items-center gap-2 rounded-xl border border-error/25 bg-error/[0.04] px-4 py-2.5 text-sm font-semibold text-error shadow-sm transition-all duration-200 hover:-translate-y-0.5 hover:border-error/50 hover:bg-error/10 hover:shadow-md active:translate-y-0 active:scale-95"><span class="transition-transform duration-200 group-hover:rotate-12">×</span><span>Remove profile</span></button></form></div>@endif</div>
@endsection
